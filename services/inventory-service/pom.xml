<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.pilarestilo</groupId>
  <artifactId>inventory-service</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>inventory-service</name>
  <description>Pilar Estilo - Extracted Inventory Read Service</description>

  <properties>
    <!--
      Patch versions pinned over the ones Spring Boot 4.0.7 brings, each because OSV-Scanner
      reports a CVE in the version the BOM pins. Overriding the property is the mechanism the
      parent offers for exactly this, and it keeps the rest of the platform where it was tested —
      a minor bump of Boot itself is a bigger change than the fixes are worth.

      Remove an override once Boot ships a version at or above it; leaving one behind pins the
      platform to an old library nobody chose.

        netty 4.2.15   -> GHSA-558v-64gr-wgg4 (8.7), GHSA-jppx-w49h-x2qq (8.7) and six more
        postgresql 42.7.11 -> GHSA-j92g-9f8w-j867 (8.2)
        jackson 3.1.4 / 2.21.4 -> GHSA-5gvw-p9qm-jgwh, GHSA-mhm7-754m-9p8w (6.5)
        log4j-api 2.25.4   -> GHSA-qv9r-c865-cp47 (6.3)
    -->
    <netty.version>4.2.17.Final</netty.version>
    <postgresql.version>42.7.13</postgresql.version>
    <jackson-bom.version>3.1.6</jackson-bom.version>
    <jackson-2-bom.version>2.21.6</jackson-2-bom.version>
    <log4j2.version>2.25.5</log4j2.version>
    <opentelemetry.version>1.62.0</opentelemetry.version>
    <!-- Coverage read by SonarQube. Written by jacoco at verify; without this Sonar reports 0%
         and every quality gate on new code passes for the wrong reason. -->
    <sonar.coverage.jacoco.xmlReportPaths>${project.basedir}/target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
    <java.version>25</java.version>
    <jacoco.version>0.8.14</jacoco.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-micrometer-tracing-opentelemetry</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-opentelemetry</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Boot 4 moved the MockMvc/@WebMvcTest slice out of spring-boot-test-autoconfigure
         into its own module (org.springframework.boot.webmvc.test.autoconfigure). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Lombok -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <configuration>
          <release>25</release>
          <annotationProcessorPathsUseDepMgmt>true</annotationProcessorPathsUseDepMgmt>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.50</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Security analysis Sonar Community cannot do.

      Community Build has no taint analysis, so it never follows a user input through to a SQL
      query: it only matches shapes. Find Security Bugs works on the compiled bytecode and does
      follow it, covering SQL/HQL injection, XSS, path traversal, weak crypto and around a hundred
      more. Free (LGPL), and the gap it fills is the one Sonar's own banner points at.

      Its own profile so it does not sit in every `mvn verify`: it needs the classes compiled and
      takes minutes. Run it deliberately:

          mvn -P security verify

      Fails the build on anything it finds, which is the point — see scripts/quality/README.md for
      how to read and silence a false positive properly.
    -->
    <profile>
      <id>security</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <version>4.9.8.0</version>
            <configuration>
              <effort>Max</effort>
              <threshold>Low</threshold>
              <!-- Only the security detectors: SpotBugs' general rules overlap with Sonar's, and
                   two tools reporting the same smell twice is noise nobody reads. -->
              <includeFilterFile>${project.basedir}/../../config/spotbugs-security.xml</includeFilterFile>
              <excludeFilterFile>${project.basedir}/../../config/spotbugs-noise.xml</excludeFilterFile>
              <plugins>
                <plugin>
                  <groupId>com.h3xstream.findsecbugs</groupId>
                  <artifactId>findsecbugs-plugin</artifactId>
                  <version>1.14.0</version>
                </plugin>
              </plugins>
            </configuration>
            <executions>
              <execution>
                <id>find-security-bugs</id>
                <phase>verify</phase>
                <goals>
                  <goal>check</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
